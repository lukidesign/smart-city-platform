# RunDo Energy Carbon Center — 开发进度报告

**文档版本：** v1.3
**报告日期：** 2026-04-09
**当前平台版本：** v3.6.0
**主文件：** `index-standalone.html`
**Figma 设计稿：** [Des_CC_FigmaBuilding3D](https://www.figma.com/design/zOsnUZun5PyXPgfz926Agg/)
**线上地址：** https://lukidesign.github.io/rundo-energy-carbon-center/index-standalone.html

---

## 一、项目概况

### 1.1 项目定位

RunDo Energy Carbon Center（原 Smart City Platform）面向建筑能耗管理场景，提供多层级视图（园区 → 建筑 → 楼层）的能耗可视化、AI 优化建议、设备监测及 ECM 系统集成功能。

### 1.2 技术架构

| 层级 | 技术选型 |
|------|---------|
| 框架 | React 18（UMD，Babel Standalone 浏览器端 JSX 编译） |
| 3D 渲染 | Three.js（WebGL，用于 Globe3D 地球仪与楼层模型） |
| 图表 | Chart.js 4.4.0 + chartjs-plugin-datalabels 2.2.0 |
| 部署形式 | **单文件 HTML**（zero-build，无需构建工具即可运行） |
| 设计同步 | Figma MCP 插件（html-to-design capture） |
| 版本管理 | Git → GitHub（lukidesign/rundo-energy-carbon-center） |
| 部署平台 | GitHub Pages（自动部署，push 后约 2–3 分钟生效） |

### 1.3 文件结构

```
energy-carbon-center/
├── index.html                    # 重定向页（meta refresh + location.replace()）
├── index-standalone.html         # 主文件（全部代码）
├── app-icon.png                  # 右上角应用图标（立方体）
├── city-view.mp4                 # 启动动画视频
├── street-view.mp4               # 备用视频（未使用）
├── city-overview.png             # 启动视频海报帧
├── zone-view.png                 # Zone tab 背景图（已隐藏）
├── newbuilding3dimg/             # Building tab 楼层图片（当前使用）
│   ├── newbuilding3d-mm.png      # 建筑全貌图
│   └── newbuilding3d-l1.png … l7.png
├── build3dimg/                   # Zone tab 楼层图片（备用）
├── zbuilding3dimg/               # 旧版 Building 楼层图片（备用）
├── rd/                           # RunDo ECM 系统文件
│   ├── RunDo-ECM-v0.6-2026-03-31.html  # ECM 主页面
│   ├── chiller-plant.html        # 冷冻机房系统动态流程图（SVG 动画）
│   ├── view1.png                 # VIEW 卡片 1 缩略图（冷冻机房系统图）
│   ├── view2.png                 # VIEW 卡片 2 缩略图（CH-1 水泵）
│   ├── view3.png                 # VIEW 卡片 3 缩略图（冷水系统总览）
│   ├── ntimg.png                 # VIEW 卡片 3 弹窗大图
│   └── ntimg01.png               # VIEW 卡片 2 弹窗大图
└── docs/                         # 本文档目录
    ├── 需求分析文档.md / .html / .pdf
    ├── 开发进度报告.md / .html / .pdf
    └── pdf-style.css
```

---

## 二、版本历史

### v3.6.0（2026-04-09）— 当前版本
- **导航栏 Zone 选择器**：在 Tab 栏（Campus / Building / Floor）右侧添加竖向分隔线 + Zone 下拉选择框（Zone A–G，来自 ZONES 数组，pill 圆角样式，Tab 栏容器宽度 480→660px）

### v3.5.9（2026-04-09）
- **Monthly Savings Y 轴动态自适应**：原 Y 轴 max/stepSize 均为硬编码；新实现用 `log10` 求数量级，`Math.ceil` 取整后 ÷5 得均分步长，无论数据范围如何变化 Y 轴始终均匀分布 5 格

### v3.5.8（2026-04-09）
- **能耗数据联动（单一数据源）**：新增顶层常量 `YEARLY_2025 = 1_423_500`（kWh），派生 `DAILY_RATE = 3900`、节能比 `OPT_RATIO = 0.75`；`E` 对象 `{daily:3900, weekly:27300, monthly:58500, yearly:409500}`；FLOORS 按楼层面积权重动态计算；StatsCards 全部从 E 对象读取，消除硬编码数值

### v3.5.7（2026-04-09）
- **项目文件夹/仓库重命名**：本地 `smart-city-platform` → `energy-carbon-center`，GitHub `lukidesign/smart-city-platform` → `lukidesign/rundo-energy-carbon-center`
- **新增 `index.html` 重定向页**：`<meta http-equiv="refresh">` + `location.replace()` 确保 GitHub Pages 正常跳转

### v3.5.6（2026-04-09）
- **冷冻机房系统动态流程图**（`rd/chiller-plant.html`）：新建独立 SVG 动态页面，展示冷冻机房完整系统拓扑；设备含 8 台冷却塔、4 台冷却泵/阀、4 台主机（特灵+格力）、4 台冷冻泵/阀、分水器/集水器/旁通阀；4 色管道流向动画（冷却水供橙/冷却水回红/冷冻水供蓝/冷冻水回青）；暗色背景 `#09111f`
- **VIEW Card #1 更新**：从显示静态图 `ntimg.png` 改为在 iframe 中打开 `rd/chiller-plant.html` 动态流程图；`rd/view1.png` 缩略图同步更新
- **温度传感器标签优化**：修复 4 处温度标签重叠问题，优化点位位置与文字方向；温度点半径 3.5→4px，字号 9.5→10.5px，添加 glow 发光效果

### v3.5.5（2026-04-02）
- 页面标题由 "Smart City Platform" 改为 **"RunDo Energy Carbon Center"**

### v3.5.4（2026-04-02）
- VIEW 卡片图片从 Figma CDN 迁移至本地（`rd/view1–3.png`），解决 CDN URL 失效问题
- Floor Tab 3D 楼层高亮修复：改用 `f.id-1` 计算 Y 坐标（修复倒序高亮错位 bug）
- Tooltip 卡片位置调整至 `left: 38.5%，top: 34%`

### v3.5.3（2026-04-02）
- Tooltip 卡片多次位置微调（多轮 +10% / -20% / +5%）

### v3.5.0（2026-04-01）— 封版里程碑
- **Zone Tab 隐藏**：从 Tab 列表移除（`{false&&tab==='Zone'&&...}`），内容代码保留
- **Building Tab 移除 Main Hospital Building**：标签及连接线仅保留在 Zone Tab
- **Building 默认楼层**：改为 **5F**（`useState(5)`）
- **Tooltip 常驻显示**：不再需要 hover 触发，始终展示在固定位置
- **Tooltip 移除对角连接线**：去掉 SVG 虚线连接器
- **GitHub 仓库重命名**：`smart-city-platform` → `rundo-energy-carbon-center`
  - 线上地址更新为：`https://lukidesign.github.io/rundo-energy-carbon-center/`

### v3.4.0（2026-04-01）
- **AI Suggestion 图标**换为六芒星 SVG（±180° 左右摇摆，3.6s 周期动效）
- **Campus Tab 默认激活**（`useState('Campus')`，原为 Building）
- **Campus 视频热区**：点击区域（23%–82% × 16%–81%）进入 Building Tab，提示文字 "Enter Building View"
- **地图控件按钮间距**恢复为 12px（原 48px）
- **全屏按钮**：文字 "Expand" → SVG 展开箭头图标
- **面板切换按钮**：矩形图标 → Layers/Stack SVG 图标
- **VIEW 卡片系统**（替换原轴承监测卡）：
  - 新增 `VIEW_CARD_DEFS` 数组（3 张：view1/2/3.png）
  - 点击卡片全屏弹窗（ReactDOM.createPortal）
  - 地图控件新增 ⊙ 图标切换 VIEW 卡片显示/隐藏（默认隐藏）
  - `bearingPulse` 光晕动效
- **Building 图片文件夹**：`zbuilding3dimg` → `newbuilding3dimg`
- **App 图标跳转**：链接改为 `rd/RunDo-ECM-v0.6-2026-03-31.html`
- **RunDo ECM 页面 Logo**：`../交互设计/logo.png` → `../app-icon.png`
- Figma 设计同步（node-id: 136-2）

### v3.3.0（2026-04-01）
- 动画套件完整实现（打字机、fadeUp、柱图、count-up、折线）
- 刷新按钮（↻）加入地图控件
- 楼层选择器淡入动效（fadeSlideIn）
- Building/Zone tab 图片切换至各自目录

### v3.2.x（2026-03-31）
- 全面对齐 Figma 1920×1080 设计稿
- UI 视觉重构（深色卡片 + 虚线连接器 + tooltip）
- 楼层悬浮位置公式修正

### v3.1.x（2026-03-30）
- Globe3D 真实地球纹理 + 星空 + 大气光晕 + 云层
- 楼层选择器顺序修正（7F 在上）
- Building tab 楼层悬停 tooltip 弹窗
- Main Hospital Building 标签可点击

### v3.0.x（2026-03-30）
- Building tab 楼层图片支持（l1–l7）
- 启动介绍优化（海报帧 + 8s 自动跳过）
- 默认 tab 切换逻辑

### v2.x（2026-03-23 至 03-30）
- Three.js Globe3D 地球仪
- AI 对话面板、多视图视频
- Campus / Building / Floor / Zone 四视图框架

### v1.x（2026-03-19 至 03-23）
- 初始单文件仪表盘
- 能耗卡片、图表、侧边栏
- Figma 设计稿同步（节点 8-2、8-266）

---

## 三、已完成功能清单

### 3.1 视图与导航

| 功能 | 状态 | 说明 |
|------|------|------|
| Campus Tab | ✅ | 城市视频循环播放 + 热区点击进入 Building |
| Building Tab | ✅ | 3D 建筑图 + 楼层选择（默认 5F）+ 常驻 Tooltip |
| Floor Tab | ✅ | Three.js 3D 建筑模型（可交互，楼层高亮联动） |
| Zone Tab | ⏸ 已隐藏 | 代码保留，随时可恢复 |
| Tab 切换动画 | ✅ | 毛玻璃样式 Tab 栏，平滑切换 |
| 启动介绍动画 | ✅ | 全屏视频 + 海报帧预加载 + 8s 自动跳过 |
| Globe3D | ✅ | Three.js 地球仪（纹理 + 星空 + 大气 + 云层） |
| Zone 导航选择器 | ✅ | Tab 栏右侧，分隔线后 Zone 下拉选择框（Zone A–G） |

### 3.2 Building Tab 交互

| 功能 | 状态 | 说明 |
|------|------|------|
| 楼层选择器 7F–1F | ✅ | 左侧竖排按钮，默认 5F 高亮 |
| 楼层图片切换 | ✅ | newbuilding3dimg 目录（含预加载 + onerror 回退） |
| Tooltip 常驻卡片 | ✅ | 固定位置 left 38.5%，显示楼层能耗数据 |
| 楼层选择器淡入动效 | ✅ | 逐按钮淡入（60ms 错开） |

### 3.3 Floor Tab 交互

| 功能 | 状态 | 说明 |
|------|------|------|
| Three.js 3D 模型 | ✅ | 多楼层着色渲染 |
| 拖拽旋转 / 滚轮缩放 | ✅ | 鼠标 + 触控支持 |
| 楼层高亮联动 | ✅ | 使用 f.id-1 计算 Y 坐标，选中楼层准确高亮 |
| 楼层选择器样式 | ✅ | 7F 在顶，pill 选中态，字号与 Building Tab 一致 |

### 3.4 左侧数据面板

| 功能 | 状态 | 说明 |
|------|------|------|
| AI Suggestion 卡片 | ✅ | 六芒星图标 + 打字机效果 + Show more 弹窗 |
| Energy Consumption 柱图 | ✅ | 双色柱，波浪增长动画，3s 实时更新 |
| Stats Cards（4张） | ✅ | 日/周/月/年用量（来自 E 对象），count-up 递增动画 |
| Monthly Savings 折线 | ✅ | 双线，从左向右绘制动画，hover 竖线，Y 轴动态自适应 |
| 面板滑入/滑出 | ✅ | translateX 动画，Layers 图标切换 |
| 能耗数据单一数据源 | ✅ | YEARLY_2025 常量派生 E 对象，FLOORS 按面积权重计算 |
| Monthly Savings 动态 Y 轴 | ✅ | log10 数量级自动计算 stepSize，均匀分布 5 格 |

### 3.5 地图控件（右下角）

| 功能 | 状态 | 说明 |
|------|------|------|
| VIEW 切换 ⊙ | ✅ | 控制 VIEW 卡片显示/隐藏，所有 Tab 可用 |
| 刷新 ↻ | ✅ | 重置 3D 选择 + 重播左侧动画 |
| 全屏 | ✅ | SVG 展开箭头图标，requestFullscreen API |
| 3D 地球仪 | ✅ | Globe3D 弹窗 |
| 面板切换 | ✅ | Layers SVG 图标 |
| 缩放 +/- | ⏳ | UI 已有，逻辑待绑定 |

### 3.6 VIEW 设备监测卡片

| 功能 | 状态 | 说明 |
|------|------|------|
| 三张卡片布局 | ✅ | VIEW_CARD_DEFS，各自高度/偏移独立配置 |
| 本地图片资源 | ✅ | rd/view1–3.png |
| 点击全屏弹窗 | ✅ | ReactDOM.createPortal |
| 呼吸光晕动效 | ✅ | bearingPulse CSS 动效 |
| 默认隐藏 | ✅ | bearingVisible 默认 false |
| 冷冻机房系统图 | ✅ | VIEW Card 1 → iframe 打开 rd/chiller-plant.html 动态流程图 |

### 3.7 RunDo ECM 集成

| 功能 | 状态 | 说明 |
|------|------|------|
| App 图标跳转 ECM | ✅ | href="rd/RunDo-ECM-v0.6-2026-03-31.html" |
| ECM 页面 Logo | ✅ | 替换为 app-icon.png（立方体图标） |

---

## 四、待开发 / 已知问题

| 项目 | 类型 | 优先级 | 说明 |
|------|------|--------|------|
| 缩放 +/- 功能绑定 | 待开发 | 中 | UI 按钮已有，需绑定视图缩放逻辑 |
| Campus Tab 数据叠加 | 待开发 | 中 | 目前仅播放视频，无热力图层 |
| 实时数据接口 | 待开发 | 高 | 目前数据为本地模拟（±5% 随机） |
| 响应式适配 | 待开发 | 低 | 当前固定 1920×1080 布局 |
| 单文件拆分重构 | 待议 | 低 | 文件体积增大，可考虑 Vite 工程化 |

---

## 五、外部依赖与资源

| 资源 | 用途 | 来源 |
|------|------|------|
| React 18 UMD | 框架 | CDN (unpkg) |
| Babel Standalone | JSX 编译 | CDN (unpkg) |
| Three.js | Globe3D + Floor 3D | CDN (unpkg) |
| Chart.js 4.4.0 | 图表 | CDN (unpkg) |
| chartjs-plugin-datalabels | 柱图数值标签 | CDN (unpkg) |
| Figma MCP capture.js | 设计稿同步 | mcp.figma.com |

---

*文档由 Claude Code 自动生成 · 2026-04-09*
