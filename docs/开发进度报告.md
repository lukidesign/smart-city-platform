# 智慧城市平台 — 开发进度报告

**文档版本：** v1.0
**报告日期：** 2026-04-01
**当前平台版本：** v3.3.0
**主文件：** `index-standalone.html`
**Figma 设计稿：** [Des_CC_FigmaBuilding3D](https://www.figma.com/design/zOsnUZun5PyXPgfz926Agg/)

---

## 一、项目概况

### 1.1 项目定位

智慧城市能耗管理平台（Smart City Platform），面向建筑能耗管理场景，提供多层级视图（园区 → 建筑 → 楼层 → 区域）的能耗可视化、AI 优化建议及数据分析功能。

### 1.2 技术架构

| 层级 | 技术选型 |
|------|---------|
| 框架 | React 18（UMD，Babel Standalone 浏览器端 JSX 编译） |
| 3D 渲染 | Three.js（WebGL，用于 Globe3D 地球仪与楼层着色） |
| 图表 | Chart.js 4.4.0 + chartjs-plugin-datalabels 2.2.0 |
| 部署形式 | **单文件 HTML**（zero-build，无需 Node/构建工具即可运行） |
| 设计同步 | Figma MCP 插件（html-to-design capture） |
| 版本管理 | Git（本地 + origin/main） |

### 1.3 文件结构

```
smart-city-platform/
├── index-standalone.html     # 主文件（全部代码）
├── build3dimg/               # Zone tab 楼层图片（build3dimg-mr.png + l1–l7）
├── zbuilding3dimg/           # Building tab 楼层图片（zbuilding3d-mr.png + l1–l7）
├── city-view.mp4             # 启动动画视频
├── city-overview.png         # 启动视频海报帧
├── app-icon.png              # 右上角应用图标
├── zone-view.png             # Zone tab 背景图
└── docs/                     # 本文档目录
```

---

## 二、版本历史

### v3.3.0（2026-04-01）— 当前版本
- **动画套件完整实现**
  - AI Suggestion 打字机效果（页面加载后 1s 开始，速度 14ms/字符）
  - 左侧模块 CSS fadeUp 逐一出现（pa1=0.1s / pa2=2.1s / pa3=2.0s / pa4=2.05s）
  - Energy Consumption 柱图波浪增长（2.1s 起，每列 500ms 延迟）
  - Stats Cards 数值 count-up（2.0s 起，easeInOut，5.15s 完成）
  - Monthly Savings 折线从左向右绘制（2.05s 起，2950ms 内完成）
- **刷新按钮**（↻）加入地图控件：重置当前 tab 3D 视图 + 重播左侧所有动画
- **楼层选择器淡入动效**（fadeSlideIn，7F→1F 每按钮 60ms 错开）
- Building tab 图片切换至 `zbuilding3dimg/` 目录
- Zone tab 图片切换至 `build3dimg/` 目录
- Figma 设计同步（node-id: 113-2）

### v3.2.x（2026-03-31）
- v3.2.1：全面对齐 Figma 1920×1080 设计稿（左侧面板比例、覆层位置）
- v3.2.0：UI 视觉重构（深色卡片背景 + 虚线连接器 + tooltip 弹窗）
- Building tab overlay 精确匹配 Figma（悬浮区域、虚线连接器、tooltip）
- 楼层悬浮位置公式修正：`68-(floor-1)*8%`

### v3.1.x（2026-03-30）
- v3.1.5：Globe3D 真实地球纹理 + 星空 + 大气光晕 + 云层
- v3.1.4：楼层选择器顺序修正（7F 在上，1F 在下）
- v3.1.3：Building tab 楼层悬停绿色区域 → 能耗 tooltip 弹窗
- v3.1.2：UI 优化（毛玻璃 Tab 栏、更深卡片背景）
- v3.1.1：启动视频海报帧（city-overview.png）
- v3.1.0：Main Hospital Building 标签可点击，链接至 hokoemc 仪表盘

### v3.0.x（2026-03-30）
- v3.0.8：App 图标链接更新至 demo.ai-hvac001.cn
- v3.0.7：逐楼层图片支持（l1–l7 小写文件名）
- v3.0.5：修复 Building tab 黑屏问题（图片路径 404）
- v3.0.4：启动介绍优化（海报帧预加载 + 8s 自动跳过）
- v3.0.3：Building tab 动态楼层图片（含 onError 回退）
- v3.0.2：默认 tab 改为 Building
- v3.0.1：AI 按钮替换为 Smart City Platform 应用图标

### v2.x（2026-03-23 至 03-30）
- v2.3.1：Three.js Globe3D 地球仪（可拖拽、缩放）
- v2.0：AI 对话面板、多视图视频、3D 建筑自动居中
- 全屏启动视频（city-view.mp4）+ Skip 按钮
- Tab 切换：Campus / Building / Floor / Zone 四视图

### v1.x（2026-03-19 至 03-23）
- 初始版本：单文件仪表盘（能耗卡片、图表、侧边栏）
- Figma 设计稿同步（节点 8-2、8-266）
- TopNav 毛玻璃样式、侧边栏图标
- 城市视图切换（City / District / Street / Building）

---

## 三、已完成功能清单

### 3.1 视图与导航

| 功能 | 状态 | 说明 |
|------|------|------|
| Campus Tab | ✅ | 城市视频循环播放 |
| Building Tab | ✅ | 3D 建筑图 + 楼层选择（1F–7F）+ hover tooltip |
| Floor Tab | ✅ | Three.js 3D 建筑模型（可交互旋转/缩放） |
| Zone Tab | ✅ | 建筑图 + 楼层选择（1F–7F）+ hover tooltip |
| Tab 切换动画 | ✅ | 毛玻璃样式 Tab 栏，平滑切换 |
| 启动介绍动画 | ✅ | 全屏视频 + 海报帧预加载 + 8s 自动跳过 |
| Globe3D | ✅ | Three.js 地球仪（纹理 + 星空 + 大气 + 云层 + 拖拽/缩放） |

### 3.2 3D 建筑交互（Building & Zone Tab）

| 功能 | 状态 | 说明 |
|------|------|------|
| 楼层选择器 7F–1F | ✅ | 左侧竖排按钮，选中态高亮 |
| 楼层图片切换 | ✅ | 点击楼层加载对应图片（含预加载 + onerror 回退） |
| 楼层 Hover 绿色区域 | ✅ | 悬停触发绿色高亮带 |
| Tooltip 弹窗 | ✅ | 显示楼层名、当前用量、优化后用量、节省比例 |
| 虚线连接器 | ✅ | SVG 对角虚线连接 tooltip 与楼层区域 |
| Main Hospital Building 标签 | ✅ | 浮动标签 + 竖线连接器，可点击跳转外部系统 |
| 楼层选择器淡入动效 | ✅ | 页面/Tab 加载时逐按钮淡入（60ms 错开） |

### 3.3 左侧数据面板

| 功能 | 状态 | 说明 |
|------|------|------|
| AI Suggestion 卡片 | ✅ | 打字机文字效果、Show more 弹窗、Dismiss / Apply 按钮 |
| AI Detail 弹窗 | ✅ | ReactDOM.createPortal 渲染（避免 transform 层叠问题） |
| Energy Consumption 柱图 | ✅ | Chart.js，双色柱（当前/优化），波浪增长动画，datalabels |
| Stats Cards（4张） | ✅ | 日/周/月/年用量，count-up 数值递增动画 |
| Monthly Savings 折线 | ✅ | Chart.js，双线（节省成本/能源），从左向右绘制动画，hover 竖线 |
| 面板滑入/滑出 | ✅ | 左侧面板 translateX 动画，由地图图标切换 |

### 3.4 地图控件（右下角）

| 功能 | 状态 | 说明 |
|------|------|------|
| 刷新按钮 ↻ | ✅ | 重置当前 Tab 3D 选择 + 左侧面板动画重播（旋转动效） |
| 缩放 +/- | ✅（UI） | 按钮已有 UI，逻辑待绑定 |
| 全屏切换 | ✅ | requestFullscreen API |
| 3D 按钮 | ✅ | 打开 Globe3D 地球仪弹窗 |
| 地图图标 | ✅ | 切换左侧面板显示/隐藏 |

### 3.5 动画系统

| 动画 | 触发时机 | 时长 |
|------|---------|------|
| AI Suggestion 打字机 | 页面加载 +1s | ~3s（14ms/字符） |
| 模块 fadeUp（pa1） | 立即（0.1s） | 0.6s |
| 模块 fadeUp（pa2/Energy Chart） | 2.1s | 0.6s |
| 模块 fadeUp（pa3/Stats Cards） | 2.0s | 0.6s |
| 模块 fadeUp（pa4/Monthly Savings）| 2.05s | 0.6s |
| 柱图波浪增长 | 2.1s | 每列 +500ms |
| Stats Cards count-up | 2.0s | 3150ms（至 5.15s） |
| 折线从左绘制 | 2.05s | 2950ms（至 5s） |
| 楼层按钮 fadeSlideIn | Tab 加载时 | 每按钮 60ms 错开 |

---

## 四、待开发 / 已知问题

| 项目 | 类型 | 优先级 | 说明 |
|------|------|--------|------|
| 缩放 +/- 功能绑定 | 待开发 | 中 | UI 按钮已有，需绑定 3D 视图缩放逻辑 |
| Campus Tab 内容 | 待开发 | 中 | 目前仅播放视频，无数据叠加层 |
| Floor Tab 楼层选择 | 待开发 | 中 | Three.js 模型已有，楼层点击高亮逻辑待实现 |
| Zone Tab 区域划分 | 待开发 | 低 | 目前 ZONES 常量保留但未使用 |
| 实时数据接口 | 待开发 | 高 | 目前数据为本地模拟（±5% 随机更新） |
| 响应式适配 | 待开发 | 低 | 当前固定 1920×1080 布局设计 |
| 单文件拆分重构 | 待议 | 低 | 文件体积增大，可考虑 Vite 工程化拆分 |

---

## 五、外部依赖与资源

| 资源 | 用途 | 来源 |
|------|------|------|
| React 18 UMD | 框架 | CDN (unpkg) |
| Babel Standalone | JSX 编译 | CDN (unpkg) |
| Three.js | Globe3D | CDN (unpkg) |
| Chart.js 4.4.0 | 图表 | CDN (unpkg) |
| chartjs-plugin-datalabels | 柱图数值标签 | CDN (unpkg) |
| hokoemc 仪表盘 | 外部跳转链接 | http://test.hokoemc.com:8060 |
| demo.ai-hvac001.cn | 应用图标跳转 | http://demo.ai-hvac001.cn |

---

*文档由 Claude Code 自动生成 · 2026-04-01*
