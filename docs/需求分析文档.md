# RunDo Energy Carbon Center — 需求分析文档（PRD）

**文档版本：** v1.3
**编写日期：** 2026-04-09
**对应平台版本：** v3.6.0
**Figma 设计稿：** [Des_CC_FigmaBuilding3D](https://www.figma.com/design/zOsnUZun5PyXPgfz926Agg/)

---

## 一、产品目标

### 1.1 核心价值

RunDo Energy Carbon Center（原 Smart City Platform）面向建筑能耗管理场景，通过多层级可视化视图（园区 → 建筑 → 楼层），帮助管理人员：

- **实时掌握**建筑各楼层能耗状态
- **直观对比**当前用量与 AI 优化后预期用量
- **快速响应**AI 建议，一键应用节能策略
- **追踪月度节省**效果（费用 / 能源 / 碳排放）
- **查看设备监测**数据（通过 VIEW 卡片联动 RunDo ECM 系统）

### 1.2 设计原则

| 原则 | 描述 |
|------|------|
| 信息密度适中 | 左侧面板承载数据，右侧视图承载空间感知 |
| 渐进式呈现 | 动画逐步引导用户注意力，避免信息轰炸 |
| 空间与数据联动 | 楼层视图点击 = 数据聚焦，保持上下文一致 |
| 深色沉浸风格 | 暗色背景 + 蓝绿荧光，符合智慧城市科技感 |
| 按需展示 | VIEW 设备卡片默认隐藏，由控制按钮显示 |

---

## 二、用户角色

| 角色 | 描述 | 核心需求 |
|------|------|---------|
| 能源管理员 | 负责整栋楼能耗管理 | 查看整体用量、应用 AI 优化建议 |
| 楼层负责人 | 负责某层楼的运营 | 查看本层用量、对比优化前后数据 |
| 设备运维员 | 关注机房/设备运行状态 | VIEW 卡片查看压缩机/泵/冷冻机房系统实时监测图 |
| 系统演示员 | 向客户展示平台能力 | 流畅动画、直观可视化、完整演示流程 |

---

## 三、功能模块需求

### 3.1 启动介绍（Intro Screen）

**功能描述：**
平台打开后播放全屏启动动画，展示城市全貌，建立产品高端感。

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 全屏视频播放 | 循环播放 city-view.mp4 | ✅ 已实现 |
| 海报帧预加载 | 视频加载前显示 city-overview.png | ✅ 已实现 |
| 自动跳过 | 8 秒后自动进入主界面 | ✅ 已实现 |
| 手动跳过 | 右下角 "Skip ›" 按钮 | ✅ 已实现 |
| 淡出过渡 | 跳过时 600ms 透明度过渡 | ✅ 已实现 |

---

### 3.2 顶部视图切换 Tab

**功能描述：**
位于右侧视图区顶部，切换不同层级的空间视图。

**当前启用的 Tab（共 3 个）：**

| Tab | 视图内容 | 数据面板 |
|-----|---------|---------|
| Campus | 城市全景视频（city-view.mp4） + 热区点击跳转 Building | 同一左侧面板 |
| Building | 3D 建筑外观图（可选楼层，默认 5F） | 同一左侧面板 |
| Floor | Three.js 交互式 3D 建筑模型 | 同一左侧面板 |

> **Zone Tab 已隐藏**（`{false&&tab==='Zone'&&...}`），内容保留在代码中，后续可复用。

**交互规范：**
- 默认激活：**Campus**
- 选中态：白色文字 + 深色背景圆角胶囊
- 未选中态：半透明白色文字
- 切换即时响应，无加载等待（图片预加载机制）
- 毛玻璃背景（backdrop-filter: blur）

**Campus Tab 热区：**
- 热区范围：`left 23%–82%，top 16%–81%`
- 点击热区：切换至 Building Tab
- 热区提示文字：`"Enter Building View"`（深色半透明气泡）

**Zone 选择器（Tab 栏右侧）：**

在 Campus / Building / Floor Tab 按钮右侧添加竖向分隔线，后接 Zone 下拉选择框。

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 位置 | Tab 栏右侧，竖向分隔线后 | ✅ 已实现 |
| 选项来源 | ZONES 数组，动态渲染 | ✅ 已实现 |
| 选项数量 | 6 个区域（Zone A–G） | ✅ 已实现 |
| 样式 | 圆角 pill，半透明深色背景，与 Tab 风格一致 | ✅ 已实现 |
| 容器宽度 | Tab 栏容器 480→660px 以容纳 Zone 控件 | ✅ 已实现 |
| 隐藏逻辑 | Zone Tab 隐藏时 dropdown 也一并隐藏 | ✅ 已实现 |

---

### 3.3 Building Tab — 3D 建筑楼层视图

**功能描述：**
展示建筑外观 3D 图，配合楼层选择器高亮对应楼层，并显示能耗数据。

#### 3.3.1 楼层选择器

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 楼层按钮列表 | 7F → 1F 竖排展示 | ✅ 已实现 |
| 默认选中楼层 | 5F | ✅ 已实现 |
| 选中高亮 | 深色背景 + 白色加粗文字 | ✅ 已实现 |
| 未选中态 | 灰蓝色文字，无背景 | ✅ 已实现 |
| 位置跟随面板 | 面板收起时按钮左移 | ✅ 已实现 |
| 淡入动效 | 页面加载时逐按钮 fadeSlide（60ms 错开） | ✅ 已实现 |

#### 3.3.2 楼层图片切换

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 默认图 | `newbuilding3dimg/newbuilding3d-mm.png`（建筑全貌） | ✅ 已实现 |
| 楼层图 | `newbuilding3dimg/newbuilding3d-l1.png … l7.png` | ✅ 已实现 |
| 预加载检测 | Image() onload/onerror，失败回退默认图 | ✅ 已实现 |

#### 3.3.3 Tooltip 卡片（常驻显示）

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 显示方式 | **常驻显示**（不需要 hover 触发） | ✅ 已实现 |
| 位置 | `left: 38.5%，top: 34%`（绝对定位于建筑图容器） | ✅ 已实现 |
| 内容 | 楼层名、当前消耗（kWh）、优化后消耗、节省百分比 | ✅ 已实现 |
| 样式 | 深色卡片，蓝色数字，绿色 badge | ✅ 已实现 |
| 对角连接线 | 已移除 | ✅ 已实现 |

#### 3.3.4 建筑标签

> Main Hospital Building 标签已从 Building Tab 移除，仅保留于 Zone Tab（当前隐藏）。

---

### 3.4 Floor Tab — Three.js 3D 建筑模型

**功能描述：**
展示可交互的 Three.js 3D 建筑模型，支持拖拽旋转和缩放，楼层高亮与选择器联动。

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 3D 模型渲染 | WebGL 渲染，多楼层着色 | ✅ 已实现 |
| 拖拽旋转 | 鼠标拖拽旋转视角 | ✅ 已实现 |
| 滚轮缩放 | 鼠标滚轮控制远近 | ✅ 已实现 |
| 触控缩放 | 两指 pinch-zoom | ✅ 已实现 |
| 楼层高亮联动 | 选中楼层与 3D 模型高亮对应（使用 `f.id-1` 计算 Y 坐标） | ✅ 已实现 |
| AI 面板联动 | AI 打开时模型自动偏移居中 | ✅ 已实现 |
| 楼层选择器顺序 | 7F 在顶，1F 在底；选中高亮 pill 样式 | ✅ 已实现 |

**3D 楼层位置计算公式：**
```js
mesh.position.y = (f.id - 1) * (FH + FG) + FH / 2
```
> 注：`f.id` 为楼层号（1–7），不使用 FLOORS 数组索引，避免倒序导致高亮错位。

---

### 3.5 Zone Tab — 区域视图（当前隐藏）

> Zone Tab 已从 Tab 列表移除并隐藏。代码中保留内容（含 Main Hospital Building 标签、区域图片集），方便后续恢复。

---

### 3.6 左侧数据面板

左侧面板宽 420px，固定在视图左侧，可通过地图图标按钮隐藏/展示（translateX 动画）。

#### 3.6.1 AI Suggestion 面板

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 建议标题 | 粗体 | ✅ 已实现 |
| 建议描述 | 打字机效果（1s 延迟后开始，14ms/字符） | ✅ 已实现 |
| 节省幅度 Badge | "+25% ↓" 绿色徽章 | ✅ 已实现 |
| Dismiss 按钮 | 关闭当前建议卡片 | ✅ 已实现 |
| Apply 按钮 | 应用建议（视觉反馈） | ✅ 已实现 |
| Show more | 展开详细分析弹窗 | ✅ 已实现 |
| AI 图标 | 六芒星 SVG，±180° 左右摇摆动效（3.6s 周期） | ✅ 已实现 |

#### 3.6.2 Energy Consumption 柱状图

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 双色柱 | 灰色（当前）+ 蓝色（优化后） | ✅ 已实现 |
| X 轴时间 | 06:00 / 12:00 / 18:00 / Now | ✅ 已实现 |
| 数值标签 | chartjs-plugin-datalabels，显示在柱顶 | ✅ 已实现 |
| 图例 | Currently / After applying AI suggestions | ✅ 已实现 |
| 波浪增长动画 | 2.1s 起，每列延迟 500ms | ✅ 已实现 |
| 实时更新 | 每 3s 随机浮动 ±5% | ✅ 已实现 |

#### 3.6.3 Stats Cards（统计卡片）

四张卡片，2×2 网格布局，数值均从 `E` 对象读取（单一数据源）：

| 卡片 | 指标 | 数值来源 |
|------|------|---------|
| Daily consumption | 日用量 | `E.daily`（3,900 kWh） |
| Weekly consumption | 周用量 | `E.weekly`（27,300 kWh） |
| Monthly consumption | 月用量 | `E.monthly`（58,500 kWh） |
| Yearly consumption | 年用量 | `E.yearly`（409,500 kWh） |

每张卡片均含：count-up 动画递增数值、优化后数值（蓝色）、节省幅度 "+25% ↓"（绿色）

#### 3.6.4 Monthly Savings 折线图

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 双折线 | Cost savings（绿色粗线）/ Energy savings（蓝色细线） | ✅ 已实现 |
| X 轴 | 1 Dec → 30 Dec（每 5 天一刻度） | ✅ 已实现 |
| 顶部数据摘要 | 4.3k 节省成本 / 2.4k kWh / 135 CO₂ | ✅ 已实现 |
| 从左向右绘制 | lineDrawPlugin 裁剪动画 | ✅ 已实现 |
| Hover 竖线 | crosshairPlugin 竖向参考线 | ✅ 已实现 |
| Y 轴动态自适应 | `_maxCost = Math.max(...SAVINGS.map(d=>d.cost))`，用 `log10` 求数量级，`Math.ceil` 取整后 ÷5 得均分步长，Y 轴始终均匀分布 5 格 | ✅ 已实现 |

---

### 3.7 地图控件（Map Controls）

右下角固定位置，提供视图操作按钮组。所有 Tab 均可操作。

| 按钮 | 图标 | 功能 | 当前状态 |
|------|------|------|---------|
| VIEW 切换 | ⊙ 圆形图标 | 控制 VIEW 设备卡片显示/隐藏（默认隐藏） | ✅ 已实现 |
| ↻ 刷新 | 旋转箭头 | 重置当前 Tab 3D 楼层选择 + 左侧面板动画重播 | ✅ 已实现 |
| + 放大 | — | 放大地图/3D 视图 | ⏳ UI 已有，逻辑待绑定 |
| − 缩小 | — | 缩小地图/3D 视图 | ⏳ UI 已有，逻辑待绑定 |
| 全屏 | SVG 展开箭头图标 | 切换全屏模式（Expand） | ✅ 已实现 |
| 3D | 地球图标 | 打开 Globe3D 地球仪弹窗 | ✅ 已实现 |
| 面板切换 | Layers/Stack SVG 图标 | 切换左侧数据面板显示/隐藏 | ✅ 已实现 |

---

### 3.8 VIEW 设备监测卡片

**功能描述：**
叠加在右侧视图区的三张设备监测图片卡片，对应真实设备数据视图，点击可全屏弹窗查看详情。

**触发方式：** 点击 Map Controls 中的 ⊙ 图标，默认隐藏。

| 卡片编号 | 缩略图 | 设备类型 | 弹窗内容 |
|---------|--------|---------|---------|
| VIEW 1 | `rd/view1.png` | 冷冻机房系统图 | iframe 打开 `rd/chiller-plant.html` 动态流程图 |
| VIEW 2 | `rd/view2.png` | CH-1 水泵监测（含 overlay 遮层） | `rd/ntimg01.png` |
| VIEW 3 | `rd/view3.png` | 冷水系统总览 | `rd/ntimg.png` |

**卡片布局参数（`VIEW_CARD_DEFS`）：**

| 卡片 | `h`（图片高度） | `top`（偏移） | `overlay` |
|------|---------------|-------------|-----------|
| 1 | 136.91% | -18.75% | false |
| 2 | 100% | 0% | true |
| 3 | 144.63% | -11.97% | false |

**交互：**
- 点击卡片 → 全屏弹窗（`ReactDOM.createPortal`），VIEW 1 在 iframe 中加载 `rd/chiller-plant.html`
- 弹窗内点击 × 或弹窗外区域关闭
- VIEW 卡片有 `bearingPulse` 呼吸光晕动效

---

### 3.9 Globe3D 地球仪

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 真实地球纹理 | Canvas 生成（含大陆轮廓、海洋渐变） | ✅ 已实现 |
| 星空背景 | 12,000 颗随机星点 | ✅ 已实现 |
| 大气光晕 | 半透明蓝色球体 | ✅ 已实现 |
| 云层 | Canvas 噪声生成，独立旋转 | ✅ 已实现 |
| 拖拽旋转 | 鼠标/触控拖拽 | ✅ 已实现 |
| 滚轮缩放 | 鼠标滚轮 / 双指缩放 | ✅ 已实现 |
| 关闭按钮 | × 按钮关闭弹窗 | ✅ 已实现 |

---

### 3.10 RunDo ECM 系统集成

| 需求项 | 描述 | 当前状态 |
|--------|------|---------|
| 右上角应用图标 | 点击跳转至 `rd/RunDo-ECM-v0.6-2026-03-31.html` | ✅ 已实现 |
| ECM 页面 Logo | RunDo ECM 顶栏左上角显示立方体 app-icon.png | ✅ 已实现 |
| ECM 页面布局 | 独立 HTML 文件，能耗碳排管理系统界面 | ✅ 已实现 |

---

### 3.11 冷冻机房系统流程图

**文件：** `rd/chiller-plant.html`（独立 HTML，SVG + CSS 动画）

**功能描述：**
展示冷冻机房完整系统拓扑，设备符号采用平面 UI 风格，管道流向以 4 色 stroke-dasharray 动画表示。

**设备清单：**

| 设备类型 | 数量 | 说明 |
|---------|------|------|
| 冷却塔 | 8 台 | 顶部排列 |
| 冷却泵 | 4 台 | — |
| 冷却阀 | 4 台 | — |
| 主机 | 4 台 | 1#2# 特灵，3#4# 格力 |
| 冷冻阀 | 4 台 | — |
| 冷冻泵 | 4 台 | — |
| 分水器 / 集水器 | 各 1 | — |
| 旁通阀 | 1 台 | — |

**管道颜色编码：**

| 管路 | 颜色 | 色值 |
|------|------|------|
| 冷却水供 | 橙色 | `#f0a020` |
| 冷却水回 | 红色 | `#e05040` |
| 冷冻水供 | 蓝色 | `#4ab8ff` |
| 冷冻水回 | 青色 | `#00c896` |

**技术规格：**
- SVG viewBox：1300×660
- 4 色 stroke-dasharray 流向动画
- 暗色背景：`#09111f`
- 温度传感器标签：点半径 4px，字号 10.5px，glow 发光效果
- VIEW Card #1 缩略图（`rd/view1.png`）同步更新

---

### 3.12 能耗数据架构（单一数据源）

**功能描述：**
所有能耗展示数值统一从顶层常量推导，消除硬编码分散数值，确保数据一致性。

**推导链：**

```
YEARLY_2025 = 1_423_500  （2025年全年用电量 kWh）
  └─ DAILY_RATE = Math.round(YEARLY_2025 / 365) = 3900
  └─ OPT_RATIO = 0.75  （节能比，节省 25%）
  └─ TODAY_YTD = 105  （截止 2026-04-15 第 105 天）
  └─ MONTHLY_DAYS = 15

E = {
  daily:   3900,
  weekly:  27300,   // 3900 × 7
  monthly: 58500,   // 3900 × 15
  yearly:  409500   // 3900 × 105
}

StatsCards ← E 对象
FLOORS     ← 按楼层面积权重动态计算（总权 1489）
```

**楼层面积权重分配：**

| 楼层 | 权重 | 日用量（kWh） |
|------|------|-------------|
| 7F | 180 | ~471 |
| 6F | 195 | ~510 |
| 5F | 220 | ~575 |
| 4F | 235 | ~615 |
| 3F | 220 | ~575 |
| 2F | 287 | ~751 |
| 1F | 152 | ~398 |
| **合计** | **1489** | **3900** |

---

## 四、交互规范

### 4.1 动画时序总览

```
0s    ──── 页面加载
0.1s  ──── pa1（AI Suggestion 卡片）fadeUp 出现
1.0s  ──── 打字机开始输入
2.0s  ──── pa3（Stats Cards）fadeUp 出现
2.0s  ──── Stats Cards 数值开始递增
2.05s ──── pa4（Monthly Savings）fadeUp 出现
2.05s ──── 折线从左开始绘制
2.1s  ──── pa2（Energy Chart）fadeUp 出现
2.1s  ──── 柱图开始波浪增长
5.0s  ──── 折线绘制完成
5.15s ──── Stats Cards 数值递增完成
```

### 4.2 颜色规范

| 用途 | 颜色值 |
|------|--------|
| 背景色 | `#07090f` |
| 卡片背景 | `rgba(24,25,31,0.90)` |
| 主文字 | `#ffffff` |
| 次要文字 | `rgba(255,255,255,0.45)` |
| 蓝色强调 | `#8098f6` |
| 绿色（节能） | `#00e5a0` |
| Tab 选中背景 | `#16171a` |
| 楼层选中按钮 | `#18191f` |

### 4.3 响应状态规范

| 元素 | Hover | Active | Selected |
|------|-------|--------|---------|
| Tab 按钮 | 文字变亮 | — | 白底 + 白字 700 |
| 楼层按钮 | — | — | 深色底 + 白字 700 |
| 地图控件按钮 | 背景略亮 | — | — |
| VIEW 卡片 | cursor pointer | — | — |

---

## 五、待开发需求（下一版本规划）

### P0 — 高优先级

| 需求 | 描述 | 预期工作量 |
|------|------|----------|
| 实时数据接口 | 接入真实能耗数据 API，替换模拟数据 | 大 |

### P1 — 中优先级

| 需求 | 描述 | 预期工作量 |
|------|------|----------|
| 缩放按钮逻辑 | +/- 按钮绑定 Building/3D 视图缩放 | 小 |
| Campus Tab 数据叠加 | 城市视频上叠加区域能耗热力图 | 大 |

### P2 — 低优先级

| 需求 | 描述 | 预期工作量 |
|------|------|----------|
| 响应式适配 | 支持 1440px / 1280px / iPad 分辨率 | 中 |
| 多语言支持 | 中英文切换 | 小 |
| 单文件工程化拆分 | Vite + TypeScript 工程结构 | 大 |
| 历史数据对比 | 月/季/年能耗趋势对比 | 中 |

---

## 六、设计资源

| 资源 | 链接 |
|------|------|
| Figma 主设计稿 | https://www.figma.com/design/zOsnUZun5PyXPgfz926Agg/ |
| 最新同步帧（v3.4.0）| node-id: 136-2 |
| 线上访问地址 | https://lukidesign.github.io/rundo-energy-carbon-center/index-standalone.html |

---

*文档由 Claude Code 自动生成 · 2026-04-09*
